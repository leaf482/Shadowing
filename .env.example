# Copy this file to .env and fill in values before deploying

# Port the Node server listens on (Caddy/nginx proxies to this)
PORT=3000

# Absolute path to SQLite database file
# Defaults to server/shadowing.db relative to server/index.js if not set
# SQLITE_PATH=/home/ubuntu/shadowing/server/shadowing.db

# Shadowing request cooldown in days (14–21, default 14)
# COOLDOWN_DAYS=14

# Comma-separated email addresses that can edit/delete any clinic
# ADMIN_EMAILS=admin@example.edu,another-admin@example.edu

# --- Session/auth policy tuning (all optional) ---
# SESSION_TTL_MS=604800000
# SESSION_REFRESH_THRESHOLD_MS=86400000
# LOGIN_RATE_WINDOW_MS=600000
# LOGIN_MAX_ATTEMPTS_PER_IP=10
# LOGIN_MAX_ATTEMPTS_PER_ACCOUNT=10
# VERIFICATION_TTL_MS=600000
# VERIFICATION_RESEND_COOLDOWN_MS=60000
# VERIFICATION_MAX_ATTEMPTS=5
# VERIFICATION_LOCK_MS=600000
# AUTH_LOGGING_ENABLED=true

# If your reverse proxy already sets X-Request-Id, it is reused; otherwise server generates one.

# --- Email verification (Resend) ---
# Sign up at https://resend.com — free tier: 3,000 emails/month
# Verify your domain at: https://resend.com/domains
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx

# The "From" address shown to users (must match your verified domain in Resend)
FROM_EMAIL=Shadow Network <noreply@shadowingnetwork.com>
